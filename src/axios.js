import * as axios from '/node_modules/axios/dist/axios.min.js'

const state = {
    POST : 'POST',
    GET : 'GET',
    URL : 'https://localhost:5001/api/v1/'
}

export default {
    state,
    axios
}